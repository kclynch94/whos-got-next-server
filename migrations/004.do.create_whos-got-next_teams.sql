CREATE TABLE teams (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    team_name TEXT NOT NULL,
    players TEXT[] NOT NULL,
    court_id INTEGER
        REFERENCES courts(id) ON DELETE CASCADE NOT NULL
);