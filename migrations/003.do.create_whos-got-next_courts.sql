CREATE TABLE courts (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    court_name TEXT NOT NULL,
    activeGame BOOLEAN NOT NULL,
    facility_id INTEGER
        REFERENCES facilities(id) ON DELETE CASCADE NOT NULL
);